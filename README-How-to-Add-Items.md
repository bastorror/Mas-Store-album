# 📝 วิธีการเพิ่มขนมใหม่ใน Mas Store

## 🎯 วิธีการเพิ่มขนมใหม่

### 1. เปิดไฟล์ `snacks-data.json`
- ไฟล์นี้เก็บข้อมูลขนมทั้งหมด
- มีโครงสร้างเป็น categories และ items

### 2. โครงสร้างข้อมูล

```json
{
  "categories": {
    "หมวดหมู่": {
      "name": "ชื่อหมวดหมู่ภาษาไทย",
      "emoji": "🆕",
      "items": [
        {
          "id": "รหัสขนม",
          "name": "ชื่อขนม",
          "description": "คำอธิบายขนม",
          "emoji": "🍪",
          "image": "images/รหัสขนม.png",
          "price": "฿50"
        }
      ]
    }
  }
}
```

## 🔧 วิธีเพิ่มขนมใหม่

### A. เพิ่มขนมในหมวดที่มีอยู่แล้ว

1. เลือกหมวดที่ต้องการ เช่น `"chips"`, `"sweets"`, `"nuts"`, `"cookies"`, `"drinks"`
2. เพิ่มข้อมูลใหม่ใน `"items"` array:

```json
{
  "id": "chips-5",
  "name": "ขนมใหม่ของคุณ",
  "description": "คำอธิบายขนม",
  "emoji": "🆕",
  "image": "images/chips-5.png",
  "price": "฿45"
}
```

### B. เพิ่มหมวดหมู่ใหม่

1. เพิ่มหมวดใหม่ใน `"categories"`:

```json
"newcategory": {
  "name": "หมวดใหม่",
  "emoji": "🆕",
  "items": [
    {
      "id": "newcategory-1",
      "name": "ขนมแรก",
      "description": "คำอธิบาย",
      "emoji": "🍎",
      "price": "฿30"
    }
  ]
}
```

2. เพิ่มปุ่มใน navigation (ไฟล์ index.html):
```html
<button class="nav-btn" onclick="showCategory('newcategory')">หมวดใหม่</button>
```

## 📋 ตัวอย่างการเพิ่มขนม

### เพิ่มขนม "โดนัทช็อกโกแลต" ในหมวดขนมหวาน:

```json
{
  "id": "sweets-5",
  "name": "โดนัทช็อกโกแลต",
  "description": "โดนัทนุ่มเคลือบช็อกโกแลตเข้มข้น",
  "emoji": "🍩",
  "image": "images/sweets-5.png",
  "price": "฿35"
}
```

## 🖼️ การใช้ภาพ PNG

### A. เพิ่มภาพใหม่
1. **เตรียมภาพ PNG**: ขนาดแนะนำ 400x400 หรือ 500x500 pixels
2. **ใส่ในโฟลเดอร์ images/**: วางไฟล์ใน `f:\MyWebsite\Mas-Store-album\images\`
3. **ตั้งชื่อไฟล์**: ใช้รูปแบบ `หมวด-ตัวเลข.png` เช่น `chips-5.png`
4. **อัปเดต JSON**: เพิ่ม `"image": "images/ชื่อไฟล์.png"` ในข้อมูลขนม

### B. ระบบ Fallback
- **มีภาพ**: แสดงภาพ PNG
- **ไม่มีภาพ**: แสดง emoji แทน
- **ภาพโหลดไม่ได้**: แสดง emoji แทนอัตโนมัติ

### C. รายการภาพที่ต้องการ
ดูรายละเอียดใน `images/README-Image-List.md`

## 🎨 Emoji ที่แนะนำ

- 🍪 คุกกี้
- 🍩 โดนัท
- 🧁 คัพเค้ก
- 🍰 เค้ก
- 🍭 อมยิ้ม
- 🍬 ลูกอม
- 🥨 เพรทเซล
- 🍿 ป๊อปคอร์น
- 🥤 เครื่องดื่ม
- 🧃 น้ำผลไม้

## ⚠️ หมายเหตุสำคัญ

1. **รหัส ID**: ใช้รูปแบบ `หมวด-ตัวเลข` เช่น `chips-5`, `sweets-6`
2. **เครื่องหมาย**: อย่าลืมใส่ comma (,) หลังแต่ละ item (ยกเว้นตัวสุดท้าย)
3. **การบันทึก**: บันทึกไฟล์ในรูปแบบ UTF-8 เพื่อรองรับภาษาไทย
4. **การทดสอบ**: รีเฟรชหน้าเว็บเพื่อดูผลลัพธ์
5. **ภาพ PNG**: ขนาดแนะนำ 400x400px, รูปแบบ PNG, พื้นหลังโปร่งใส
6. **ชื่อไฟล์ภาพ**: ต้องตรงกับที่ระบุใน `"image"` field

## 🚀 เคล็ดลับ

- ใช้ VS Code หรือ text editor ที่รองรับ JSON
- ใช้ online JSON validator เพื่อตรวจสอบ syntax
- สำรองไฟล์ก่อนแก้ไขเสมอ
- ทดสอบเพิ่มทีละรายการเพื่อป้องกันข้อผิดพลาด

---

**ตัวอย่างไฟล์ที่สมบูรณ์อยู่ใน `snacks-data.json`**
